/// The legacy laser wire value remains readable; new controllers start in
/// joystick mode and expose their aiming pad inside that normal layout.
enum RemoteControllerMode { joystick, laser, driving }
